pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{ts,tsx}'
      run: cd packages/client && npm run lint
    prettier:
      glob: '*.{ts,tsx,css}'
      run: cd packages/client && npm run format
      stage_fixed: true
